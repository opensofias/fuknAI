const put_on = (lower, upper) => {
	find_space
	grasp
	move
	ungrasp
} 

const grasp = box =>{
	clear_top }

const clear_top = box => {
	get_rid }

const get_rid = box => {
	put_on
}

find_space = () => {
	get_rid
}